import React from 'react';
import ButtonSkip from '../ButtonSkip/ButtonSkip';
import './FileInvite.css';

const FileInvite = () => {
  return (
    <main className="file-invite-main">
      <div className="file-invite-container">
        <div className="file-invite-page-image"></div>
      </div>
      <ButtonSkip />
    </main>
  );
};

export default FileInvite;
